{
  "name": "kai",
  "version": "0.14.0",
  "description": "Skills + scripts wired in this repo. Node deps power web-content-extraction; lectoria (used by generate-audio) is pinned as a git dependency and built on install via its prepare hook — run `npm install` at the plugin root. See skills/generate-audio/SKILL.md.",
  "private": true,
  "scripts": {
    "validate": "node scripts/validate-plugin.mjs",
    "doctor": "node scripts/workspace-doctor.mjs",
    "doctor:self-test": "node scripts/workspace-doctor.mjs --self-test",
    "host-contract": "node scripts/host-contract.mjs --self-test",
    "host-contract:update": "node scripts/host-contract.mjs --update",
    "test": "node scripts/validate-plugin.mjs && node scripts/workspace-doctor.mjs --self-test && node scripts/host-contract.mjs --self-test",
    "extract:path": "node scripts/extract-learn-path.js",
    "extract:module": "node scripts/extract-learn-path.js --module"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  },
  "dependencies": {
    "lectoria": "github:RubenSaucedo/lectoria"
  }
}
