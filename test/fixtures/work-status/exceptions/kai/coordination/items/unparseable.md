This item record is intentionally malformed: it has no YAML frontmatter at all.

A reader cannot determine its state, owner, or lease. The status report must
surface it as an integrity failure rather than skipping it silently, because a
silently skipped record is invisible work.
