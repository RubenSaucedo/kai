---
name: tools-not-array
description: A skill whose tools value is a bare scalar instead of an inline array.
tools: view
user-invocable: false
---

# tools-not-array (malformed fixture)

Proves the loader mirror rejects a `tools` value that is not an inline array.
Do not "fix" it — it must stay malformed.
