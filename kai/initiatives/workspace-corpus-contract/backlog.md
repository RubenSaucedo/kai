# workspace-corpus-contract — backlog

Initiative-scoped deferred proposals — the `proposal_channel` named in
`northstar.md`. An expansion discovered inside `workspace-corpus-contract`
lands here, not in `kai/coordination/backlog.md`, which is for unaffiliated
proposals. This file is the **one-way valve** `kai-core-scope-discipline`'s
deferrals flow into; only the steward opens it outward, by promoting an entry
to an authoritative `kai/coordination/items/*.md` record when it fits
`scope.current`. See `kai-core-initiative-stewardship`.

**Nothing parked yet.** This initiative's four founding items
(`area-plugins-workspace-storage-modes`, `area-plugins-initiative-archive`,
`area-plugins-backlog-contract`, `area-plugins-design-output-contract`) were
created directly as `proposed` items by the steward during the
`area-plugins` split (2026-08-27-2113), so no backlog entry exists for any of
them and none should be added — the item records are the record.

Two concerns are explicitly deferred **to those items themselves**, not parked
here, because each already has an owning item and a stated reopen condition:

- Mock specifics (`options.html` shape, screenshot locations, serving
  behavior) — owned by `area-plugins-design-output-contract`; see its thread's
  `DECISION 2026-08-27-2113` §1–§3.
- Initiative closure/archive mechanics beyond the 13-section archive contract
  — owned by `area-plugins-initiative-archive`; see its thread's
  `DECISION 2026-08-27-2113` §7 and §11, and its own `backlog.md`-equivalent
  once that item reaches implementation.

The per-lane hybrid storage mode remains deferred with a trigger inside
`area-plugins-workspace-storage-modes`' own thread (requirement R4), not
duplicated here, to avoid the same idea living in two places per the backlog
contract's own item/backlog exclusivity rule.
