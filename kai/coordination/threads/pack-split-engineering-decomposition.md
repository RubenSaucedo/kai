# Thread — pack-split-engineering-decomposition

Append-only communication log mirroring
`kai/coordination/items/pack-split-engineering-decomposition.md`. Never edited
after the fact — only appended. See `kai-core-work-coordination`.

## HANDOFF 2026-08-24-1831 — principal-product-manager -> principal-swe-manager

- did:       Created this `proposed`, **gated** decomposition item as the initiative's real `principal-swe-manager` consultation touchpoint. As steward I set the milestone `required_items` closure contract (the capability buckets, derived from the operator-approved corrections and the Opus 5 CHANGES-REQUIRED list), but the detailed sizing, sequencing, parallelization, and PR boundaries — and the creation of the downstream `proposed` records — are the manager's decomposition, deliberately not authored here. No `principal-swe-manager` position has been simulated.
- state:     proposed
- needs:     Once `pack-split-partition-lock` is `completed`, the steward promotes this to `ready`; then produce the decomposition record at `kai/initiatives/pack-split/artifacts/decisions/pack-split-engineering-decomposition.md` and one `proposed` work-item record per `required_items` ID on the `dependency-guarantees`, `first-pack-extracted`, and `five-pack-split-shipped` milestones — each with outcome, acceptance, `delivery_class`, typed `depends_on`, `touches`, `artifact_target`, and `review_requirements` — sequenced so the non-negotiables hold (generate-not-move, uninstall-first/coexistence-forbidden, one department at a time, groundwork on 0.x, 1.0.0 only at the staged 12a/12b/12c flip).
- artifacts: kai/initiatives/pack-split/northstar.md (milestone required_items closure contract); kai/coordination/items/pack-split-engineering-decomposition.md
- evidence:  56 agents on disk and the five-pack partition summing 7+20+9+11+9 = 56, enforced disjoint by `node scripts/pack-preview.mjs --self-test`; 50 skills on disk with 22 `kai-core-*` prefixed and one version-pinned (`kai-core-contract-v1`) — captured 2026-08-24 from C:\src\kai
- questions: none
- next:      principal-swe-manager — owns the how-it's-sliced decomposition, but only after the partition locks. Until then this item is `proposed` and not dispatchable; `pack-split-partition-lock` is the single `ready` item.

## HANDOFF 2026-08-24-1959 — principal-product-manager -> principal-swe-manager

- did:       **Gated promotion executed — `proposed -> ready`.** The dependency is genuinely satisfied: `pack-split-partition-lock` is `completed` (accepted by the owning role; the single required `independent-architecture` review is RATIFIED against `change_ref fd44f4fdf4a2b0c1e5e70f8369bd2a0b45a8224e`, and the operator's live `pack-preview.mjs` run matches the locked partition bit-for-bit). As steward I promoted this item to `ready` (priority 20 -> 10 — the single next executable action for the initiative), kept `next_role: principal-swe-manager`, incremented version 1 -> 2, and carried three non-blocking architect caveats into the item's Notes so the decomposition addresses them.
- state:     ready
- needs:     Produce the decomposition record at `kai/initiatives/pack-split/artifacts/decisions/pack-split-engineering-decomposition.md` and one `proposed` work-item record per `required_items` ID on the `dependency-guarantees`, `first-pack-extracted`, and `five-pack-split-shipped` milestones — each with outcome, acceptance, `delivery_class`, typed `depends_on`, `touches`, `artifact_target`, and `review_requirements` — sized/sequenced/parallel-aware so the non-negotiables hold (generate-not-move, uninstall-first/coexistence-forbidden, one department at a time, groundwork on 0.x, 1.0.0 only at the staged 12a/12b/12c flip). Consume the locked partition; do **not** re-derive or change it — route any needed partition change back to the steward as a question.
- caveats to address (from the ratified partition-lock review, non-blocking): (a) rename `fleet-observation` -> `kai-core-fleet-observation` under `dependency-guarantees` — **forced** (CI prefix check fails until renamed); place the required rename in that milestone's item set; (b) decide whether to bind the 3 review lenses (`review-dependencies`/`review-performance-scale`/`review-success-metrics`) on `workflow-doc-review`'s `**Inherits:**` line — firing path, not owner; (c) adopt the explicit asset-ownership rule (a non-markdown asset travels with the sole skill that invokes it; any asset invoked across >1 pack promotes to core) in `pack-split-generated-pack-trees`.
- artifacts: kai/coordination/items/pack-split-engineering-decomposition.md (version 2, `ready`, priority 10); kai/initiatives/pack-split/northstar.md (`scope.current: dependency-guarantees`); kai/initiatives/pack-split/log.md
- evidence:  `pack-split-partition-lock` = `completed` (version 9, lease null) with ratified `independent-architecture` review bound to `change_ref fd44f4f…`; locked partition at kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md — captured 2026-08-24 from C:\src\kai
- questions: none
- next:      principal-swe-manager — author the decomposition and the downstream `proposed` records; the steward grooms/prioritizes/promotes them milestone-by-milestone as they return. Dispatch is the director's (`director-chief-of-staff`) to make now that the item is `ready`.

## HANDOFF 2026-08-24-2012 — principal-swe-manager -> principal-product-manager

- did:       **Claimed** the `ready` item (self-grant as sole acting agent, no director: version 2 -> 3, `ready -> in-progress`, token `swe-mgr-2026-08-24-2011-b7c4`), then **authored the decomposition** and the downstream `proposed` records, then cleared the lease and handed to the steward (version 3 -> 4). Produced: (1) the canonical plan at `kai/initiatives/pack-split/artifacts/decisions/pack-split-engineering-decomposition.md` — 14 sized/sequenced workstreams (12 required IDs + 2 manager splits), critical path, versioning & closure table, proposed `required_items` mapping, 2 scope negotiations, 4 open questions; (2) one `proposed` item record + mirrored thread per workstream. Consumed the locked partition; did **not** re-derive or alter it. No plugin/CI/marketplace change; no branch/commit/PR/tag/release; no host gate executed.
- state:     in-progress (authoring complete; lease cleared; awaiting steward acceptance)
- needs:     Steward (`principal-product-manager`) — **accept the decomposition** against the item's 4 acceptance criteria and the closure contract; confirm the proposed `required_items` update (add `pack-split-generator-gates` shipped to `dependency-guarantees`); decide the 2 scope questions (host-gate depth; keep-vs-fold the host-semantics spike); then groom + promote the **single first implementation item** (`pack-split-generator-gates`) to `ready` and complete this knowledge item. Route the review-lens binding (caveat b) to `principal-swe-architect` — I did not guess it.
- caveat placement: (a) fleet rename -> `pack-split-ci-partition-checks` (dependency-guarantees, forced by the prefix CI); (b) review-lens binding -> routed to architect, deferred to the engineering tree in `pack-split-release-12c` (NOT on the personal-first critical path); (c) asset-ownership rule -> `pack-split-generated-pack-trees`.
- artifacts: kai/initiatives/pack-split/artifacts/decisions/pack-split-engineering-decomposition.md; 14 items under kai/coordination/items/pack-split-*.md; 14 threads under kai/coordination/threads/pack-split-*.md; kai/coordination/items/pack-split-engineering-decomposition.md (version 4)
- evidence:  56 agents / 50 skills partition consumed from the locked artifact (§3–§7); scripts/pack-preview.mjs, validate-plugin.mjs, release-guard.mjs, workspace-doctor.mjs, generate-catalog.mjs, hooks.json read to ground sizing; fleet-observation + demo-*.mjs + observe-*.mjs blast radii grepped — captured 2026-08-24 from C:\src\kai
- questions: 2 scope decisions + 4 open questions for the steward/architect (see the decomposition record); none blocks the first item
- next:      principal-product-manager — accept, groom, promote `pack-split-generator-gates` to `ready`, and complete this item; then `director-chief-of-staff` dispatches the first item.

## HANDOFF 2026-08-24-2013 — principal-product-manager -> director-chief-of-staff

- did:       **Accepted the decomposition and closed this knowledge item `completed`** (version 4 -> 5, lease null, `next_role: director-chief-of-staff`). Acceptance verified against the item's four criteria, not rubber-stamped: (1) all 12 `required_items` IDs have `proposed` records with outcome/acceptance/`delivery_class`/typed `depends_on`/`touches`/`artifact_target`/`review_requirements` — plus 2 justified manager splits; (2) sizing/sequencing/parallelization are recorded with rationale and the PR order preserves every non-negotiable (generate-not-move, uninstall-first/coexistence-forbidden, one department at a time, groundwork on `0.x`, `1.0.0` only at the staged 12a/12b/12c flip); (3) the locked partition is consumed, not re-derived, and the review-lens binding is routed to the architect; (4) no generator/agent/CI/marketplace change was made. **Steward decisions recorded:** accepted the `generator-gates` split and added it to `dependency-guarantees` `required_items` (shipped); on the 2 scope questions — **host-gates = minimal smoke gate, not full certification** (aligns with the northstar `deferred`), and **keep `host-semantics-spike` as a separate gating spike** (its one operator session can save the extraction investment). Routed the review-lens binding (caveat b) to `principal-swe-architect`; it is off the personal-first critical path.
- state:     completed
- needs:     Director dispatches the single `ready` implementation item `pack-split-generator-gates` to `principal-swe-infra`; recommend also promoting/dispatching `pack-split-host-semantics-spike` to `@operator` to run in parallel. The other 12 records stay `proposed` for milestone-by-milestone grooming as they return.
- artifacts: kai/initiatives/pack-split/artifacts/decisions/pack-split-engineering-decomposition.md; kai/coordination/items/pack-split-engineering-decomposition.md (version 5, completed); kai/initiatives/pack-split/{northstar.md,deliverables.md,log.md}; kai/coordination/{ACTIVE.md,BOARD.md}
- evidence:  14 item+thread pairs on disk under kai/coordination/; `dependency-guarantees.required_items` now 5 items incl. generator-gates — captured 2026-08-24 from C:\src\kai
- questions: none blocking; 4 open questions carried on the decomposition record for the architect/steward as the chain is groomed
- next:      director-chief-of-staff — dispatch `pack-split-generator-gates` (ready, priority 10) to `principal-swe-infra`.
