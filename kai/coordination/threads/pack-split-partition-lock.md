# Thread — pack-split-partition-lock

Append-only communication log mirroring
`kai/coordination/items/pack-split-partition-lock.md`. Never edited after the
fact — only appended. See `kai-core-work-coordination`.

## HANDOFF 2026-08-24-1817 — workflow-initiative-init -> principal-product-manager

- did:       Created the pack-split initiative (`status: proposed`) from the committed `docs/proposals/pack-architecture.md` — thin north star, four milestones (`partition-lock`, `dependency-guarantees`, `first-pack-extracted`, `five-pack-split-shipped`), four success measures, seven non-negotiable principles — and seeded this one `proposed`, docs-only planning item (`required_for_milestone: false`) on the `partition-lock` milestone. Added the initiative row to `kai/initiatives/INDEX.md`.
- state:     proposed
- needs:     Steward scope approval before anything executes — (1) confirm mission, scope, and non-negotiables; (2) accept or revise the four milestones; (3) after decomposition with `principal-swe-manager`, approve an explicit, non-empty, typed `required_items` mapping per milestone (`completed` for docs/decision outputs, `shipped` for production moves); (4) set this item's review contract, prioritize, and promote it to `ready`; (5) dispatch it to its recommended executor, `principal-technical-writer`, to produce the partition-lock doc at `kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md`.
- artifacts: kai/initiatives/pack-split/northstar.md; kai/initiatives/pack-split/log.md; kai/initiatives/pack-split/backlog.md; kai/initiatives/pack-split/deliverables.md; kai/coordination/items/pack-split-partition-lock.md
- evidence:  docs/proposals/pack-architecture.md (committed decisions and measured host behaviour); scripts/pack-preview.mjs (computes and self-tests the partition via --all); .kai/manifest.json (workspace mode repository, root ".", schema v2) — all captured 2026-08-24-1817 from the repo at C:\src\kai
- questions: none
- next:      principal-product-manager — owns pack-split as steward; on approval, sets `status: active` and adds the slug to `kai/coordination/ACTIVE.md`. Preserve the role boundary: the partition-lock doc feeds engineering decomposition (`principal-swe-manager`); production moves happen only after the guarantees are accepted. This is a developer-facing packaging change with no user-facing interaction surface, so the product-design step is not implicated unless the steward finds otherwise.

## HANDOFF 2026-08-24-1831 — principal-product-manager -> director-chief-of-staff

- did:       **Scope gate passed — initiative APPROVED and activated.** Validated mission, scope, principles, success measures, and milestones against repository evidence (56 agents on disk; the five-pack partition sums 7+20+9+11+9 = 56 and is enforced disjoint by `node scripts/pack-preview.mjs --self-test`; 50 skills on disk with 22 `kai-core-*` prefixed and exactly one version-pinned; orphan skills confirmed uninherited). Set `northstar status: active` and carried the operator-approved corrections into the thin core — separate `kai-core` identity; **uninstall-first / coexistence-forbidden** legacy `kai`; **root `agents/`+`skills/` as single source of truth** with generated, committed-but-**unpublished** pack trees (generate-not-move, reframing the old "move one department" prose); and the **staged 12a/12b/12c** release. Approved a non-empty typed `required_items` mapping on all four milestones. Groomed and **promoted this item to `ready`**: flipped `required_for_milestone: true` (this locked-partition artifact is the milestone's required knowledge deliverable), priority 10, `next_role: principal-technical-writer`, one review — `principal-swe-architect` / `independent-architecture`. Created one gated `proposed` item, `pack-split-engineering-decomposition` (the real `principal-swe-manager` decomposition touchpoint), `depends_on` this item `completed`.
- state:     ready
- needs:     Dispatch **`pack-split-partition-lock`** to `principal-technical-writer` to produce the locked-partition doc at `kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md`, taking agent/skill assignments and the orphan set from live `node scripts/pack-preview.mjs --all` output (not from any hard-coded count). On `in-review`, route to `principal-swe-architect` (`independent-architecture`) against the staged `change_ref`; the owning role then verifies acceptance and moves it to `completed`. **Do not dispatch anything else** — `pack-split-engineering-decomposition` is `proposed` and gated; no generator, code, CI, or marketplace work starts until partition-lock is `completed`.
- artifacts: kai/initiatives/pack-split/northstar.md; kai/coordination/items/pack-split-partition-lock.md; kai/coordination/items/pack-split-engineering-decomposition.md; kai/coordination/threads/pack-split-engineering-decomposition.md; kai/initiatives/pack-split/log.md; kai/coordination/ACTIVE.md; kai/initiatives/INDEX.md; kai/coordination/BOARD.md
- evidence:  agents/ (56 .agent.md); skills/ (50 SKILL.md, 22 `kai-core-*`); scripts/pack-preview.mjs (`PACKS` partition + `--self-test` invariant); grep confirms `fleet-observation`, `onboard-to-codebase`, `demo-zoom`, `create-product-demo` inherited by no agent — captured 2026-08-24 from C:\src\kai
- questions: none blocking. Two operator decisions are pre-answered and recorded (separate `kai-core`; uninstall-first/coexistence-forbidden). The remaining unmeasured host gates (macOS + cloud + real install order + fresh sessions) are already scoped as `pack-split-host-gates` under `first-pack-extracted` and gate the `1.0.0` cut — not blocking the current `ready` item.
- next:      director-chief-of-staff — dispatch the single `ready` item (`pack-split-partition-lock`) to `principal-technical-writer`; hold `pack-split-engineering-decomposition` until its dependency closes. Steward (`principal-product-manager`) reconvenes to run the `principal-swe-manager` decomposition pass once the partition locks.

## HANDOFF 2026-08-24-1847 — director-chief-of-staff -> principal-technical-writer

- did:       Reconciled authoritative state and confirmed `pack-split-partition-lock` is the single **executable** item — `ready`, no lease, `depends_on: []`, `waiting_on_questions: []`, single-file `touches` non-conflicting with the only other (proposed) item, steward-approved scope/priority, explicit in-workspace `artifact_target`. Verified workspace schema-compatibility (`.kai/manifest.json` schema_version 2, repository mode, root `.`, committed) and roster anchor (56 `*.agent.md` on disk, matching the `PACKS` sum 7+20+9+11+9). **Reserved the item serially as sole grantor**: wrote the lease (holder `principal-technical-writer`, token `ptw-2026-08-24-1847-a3f9`, `version_at_grant: 2`), moved `ready -> in-progress`, incremented `version 2 -> 3`, re-read and confirmed the grant.
- state:     in-progress
- needs:     Author the authoritative partition-lock doc at `kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md`, satisfying all five acceptance criteria. Take agent/skill assignments and the orphan set from **live `node scripts/pack-preview.mjs --all` + `--self-test` output** (not any hard-coded count), and prove the invariant by diffing the recorded partition against the on-disk roster. Cover the Opus 5 review gaps in scope for a docs artifact — every skill given an explicit keep-in-core/move disposition, the `kai-core-*` naming decision with exactly one version-pinned skill, and the dependency-direction claim (core depends on nothing; no pack depends on another). Produce a reproducible `change_ref` via `git stash create` after staging the new file (a dangling review-binding object only — **do not** create a branch, PR, commit on any branch, merge, tag, or release), record it on the item, set `state: in-review`, set `next_role: principal-swe-architect`, clear the lease, and append a HANDOFF. Verify `holder`/`token`/`version` before each state-changing write.
- artifacts: kai/coordination/items/pack-split-partition-lock.md (leased, in-progress, version 3)
- evidence:  agents/ (56 `*.agent.md`); scripts/pack-preview.mjs (`PACKS` partition + `planPacks()` orphans + `--self-test` invariant); .kai/manifest.json (schema_version 2, repository, committed) — captured 2026-08-24-1847 from C:\src\kai
- questions: none
- next:      principal-technical-writer — authors the locked-partition doc; on `in-review` the director dispatches `principal-swe-architect` (`independent-architecture`) against the exact `change_ref`, then returns to `principal-product-manager` (owning role) for acceptance/closure and truthful milestone update.

## STATUS 2026-08-24-1856 — principal-technical-writer (BLOCKED before in-review: `change_ref` mint needs shell/git)

- did:       Authored the authoritative partition-lock doc at `kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md` (~30.4 KB), satisfying all five acceptance criteria. Derived the partition **two independent ways** and confirmed they agree: (A) replayed `PACKS` + `planPacks()` against the on-disk roster (56 `agents/*.agent.md` `**Inherits:**` lines, 50 `skills/*/SKILL.md` dirs); (B) the proposal's reported `--all` run (`docs/proposals/pack-architecture.md`, Phase 2/3). Recorded: 56 agents by pack (core 7 / engineering 20 / product 9 / gtm 11 / personal 9, 0 unassigned, 0 double-claimed); 50 skills by single provider (22 core-provided = 15 core-shared + 7 core-internal; engineering 11, product 3, gtm 2, personal 3; 9 orphans); an explicit disposition for each orphan (keep-in-core 1 `fleet-observation`; move 8 — `personal` +4 demo skills, `engineering` +4 `onboard-to-codebase` + 3 review lenses) with one-line rationales; the `kai-core-*` naming decision with the single version-pinned `kai-core-contract-v1` and no taxonomy segment; and the dependency-direction claim (core depends on nothing; no pack depends on another; shared skills resolve from core across the boundary). Reconstructed `--all` and `--self-test` (PASS 16/16) in artifact §8. Updated the item Evidence + checked all five acceptance boxes; version 3 -> 4; lease retained.
- state:     in-progress (deliberately **not** advanced to `in-review`).
- needs:     A **shell/git-capable actor** (director or operator, or re-dispatch me once a shell tool is available) to mint the review-binding `change_ref`, because `git stash create` requires git execution that this role's toolset (view/create/edit/grep/glob only) does not have. No SHA was fabricated. Exact steps from repo root `C:\src\kai`:
             1. `node scripts/pack-preview.mjs --self-test`  → expect `16 checks passed`, exit 0.
             2. `node scripts/pack-preview.mjs --all --out .kai/runs/pack-split/preview-evidence`  → expect the artifact §8 numbers (7/20/9/11/9; `core skills: 22 (+9 …)`; engineering 11, product 3, gtm 2, personal 3; the 9-name orphan list). Throwaway `--out` dir; not a deliverable.
             3. `git add kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md`
             4. `git stash create "review-binding: pack-split-partition-lock"`  → copy the printed 40-hex SHA (creates a dangling object only — no branch, no commit on any branch, no tag/release).
             5. `git reset HEAD kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md`  → unstage; file stays on disk.
             Then set `change_ref: <sha>`, `state: in-review`, `next_role: principal-swe-architect`, clear the lease, increment `version`, and dispatch the `independent-architecture` review to ratify §5 dispositions + §7 dependency-direction against that SHA.
- artifacts: kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md; kai/coordination/items/pack-split-partition-lock.md (version 4, Evidence filled, acceptance checked)
- evidence:  Reconstructed `--all`/`--self-test` (artifact §8) + agent roster diff (§3.6) + 50-skill accounting (§4), cross-checked against `docs/proposals/pack-architecture.md` Phase 2/3 ("7 agents, 22 core skills + 9 unplaceable … 56 of 56"). Grep-confirmed the 9 orphans appear on no `**Inherits:**` line. Reconstruction (not live `node`) is transparently labelled; the architect's live re-run is the ratifying capture.
- questions: None blocking the content or the partition. Two ratification caveats flagged for the architect, neither decided here: (1) `fleet-observation` kept in core is the one core-provided skill without the `kai-core-*` prefix — candidate for a downstream rename to preserve the provider==prefix invariant; (2) `workflow-doc-review` names `review-dependencies` / `review-performance-scale` / `review-success-metrics` in its body but not on its `**Inherits:**` line — whether to bind them is a downstream engineering-content decision.
- next:      director-chief-of-staff (or operator) — mint the `change_ref` via the steps above (or re-dispatch with a shell), then complete the flip to `in-review` and dispatch `principal-swe-architect`. Lease retained (holder `principal-technical-writer`, token `ptw-2026-08-24-1847-a3f9`) so the item is not orphaned; release/re-grant is the grantor's call. If a shell is granted back to me, I complete steps 1–5, set `in-review`, clear the lease, and append the architect HANDOFF.

## QUESTION Q-pack-split-partition-lock-01 2026-08-24-1912 — director-chief-of-staff -> @operator

- status: open
- kind: action
- blocking: yes
- context: The partition-lock artifact is authored and self-checked — all 5 acceptance criteria addressed; the partition was derived two independent ways (replaying `PACKS`+`planPacks()` against the on-disk roster, and the proposal's reported `--all` run) and they agree. The single required independent review (`principal-swe-architect`, `independent-architecture`) must bind to an exact git `change_ref`, and acceptance criteria 2–3 want the `--all`/`--self-test` invariant shown by a live run. **No agent in this environment can execute a shell** — confirmed across three dispatches: `principal-technical-writer` (no shell), a `task`-type helper (no shell), and a `general-purpose` agent that returned `NO_SHELL_AVAILABLE`. So no kai role can run `node`/`git` to mint the review-binding object or capture live script output. This is a host-capability gap, not a judgment call, and I will not fabricate a SHA or "reconstructed-as-live" output.
- ask: From `C:\src\kai`, please run these six commands and paste back (a) the self-test summary line + exit code, (b) the `--all` partition/orphan output, and (c) the printed 40-hex `change_ref` SHA:
  1. `node scripts/pack-preview.mjs --self-test`
  2. `node scripts/pack-preview.mjs --all --out .kai/runs/pack-split/preview-evidence`
  3. `git add kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md`
  4. `git stash create "review-binding: pack-split-partition-lock"`   ← copy the 40-hex SHA (a dangling object; no branch/commit-on-branch/tag)
  5. `git reset HEAD kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md`   ← unstage; file stays on disk
  6. (optional proof) `git show <SHA>:kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md | head -15`
  Alternatively, grant an agent shell access and I will run these myself.
- answer_by: next-dispatch

## HANDOFF 2026-08-24-1912 — director-chief-of-staff -> @operator

- did:       Reconciled the writer's return against durable state. Confirmed the artifact is authored on disk (`kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md`, ~30.4 KB), all 5 acceptance boxes addressed, item advanced by the author to version 4 with Evidence filled; verified the writer correctly **refused to fabricate** a `change_ref`. Attempted the mechanical mint + live verification via three shell-capable dispatches — `principal-technical-writer` (no shell), a `task` helper (no shell), and a `general-purpose` agent (returned `NO_SHELL_AVAILABLE`): **no agent in this runtime can execute `node`/`git`.** Released the writer's lease (authoring complete; no active worker while blocked), moved the item to `blocked` with `resume_state: in-progress`, `waiting_on_questions: [Q-pack-split-partition-lock-01]`, version 4 -> 5, and escalated the mint + live verification to `@operator` as blocking action `Q-pack-split-partition-lock-01`. No SHA fabricated; no in-review flip faked; no branch/commit/PR/tag/release; no plugin behaviour changed.
- state:     blocked (resume_state: in-progress)
- needs:     `@operator` runs the six commands in `Q-...-01` and pastes the self-test line, the `--all` output, and the `change_ref` SHA (or grants an agent shell). On answer: the director verifies live output equals the writer's reconstructed numbers (core 7/eng 20/prod 9/gtm 11/personal 9; 22 core-provided; 9 orphans), records `change_ref`, restores `in-progress` and moves to `in-review`, then dispatches `principal-swe-architect` (`independent-architecture`) against that SHA to ratify the §5 orphan dispositions and §7 dependency-direction claim. If clean, `principal-product-manager` verifies acceptance, moves the item to `completed`, updates the `partition-lock` milestone, and (as steward) may promote the gated `pack-split-engineering-decomposition` to `ready`.
- artifacts: kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md; kai/coordination/items/pack-split-partition-lock.md (version 5, blocked); kai/coordination/threads/pack-split-partition-lock.md; kai/coordination/BOARD.md
- evidence:  `general-purpose` dispatch returned `NO_SHELL_AVAILABLE`; `task` + `principal-technical-writer` both reported no shell — captured 2026-08-24 from C:\src\kai. Writer's two-way reconstruction cross-checked vs `docs/proposals/pack-architecture.md` Phase 2/3 ("7 agents, 22 core skills + 9 unplaceable … 56 of 56").
- questions: Q-pack-split-partition-lock-01 (open, kind: action, blocking, -> @operator)
- next:      @operator — mint `change_ref` + live verification (or grant shell); then `director-chief-of-staff` resumes the review→closure chain.

## ANSWER Q-pack-split-partition-lock-01 2026-08-24-1938 — @operator -> director-chief-of-staff

- status:    answered — resolves `Q-pack-split-partition-lock-01` (mint the review-binding `change_ref` + capture live `pack-preview.mjs` verification).
- ran:       `@operator` ran the commands from repo root `C:\src\kai`:
             1. `node scripts/pack-preview.mjs --self-test` → **exit 0**: `pack-preview self-test: 16 checks passed`.
             2. `node scripts/pack-preview.mjs --all --out .kai\runs\pack-split\preview-evidence` → **exit 0**: 7 core / 20 engineering / 9 product / 11 gtm / 9 personal agents = **56**; `core skills: 22 (+9 inherited by nobody)`; pack-owned engineering 11, product 3, gtm 2, personal 3; unplaceable by inheritance (9): `create-product-demo`, `demo-capture`, `demo-narrate`, `demo-zoom`, `fleet-observation`, `onboard-to-codebase`, `review-dependencies`, `review-performance-scale`, `review-success-metrics`.
             3–5. `git add <artifact>` → `git stash create "review-binding: pack-split-partition-lock"` → `git reset HEAD <artifact>`: printed the 40-hex dangling review-binding object `fd44f4fdf4a2b0c1e5e70f8369bd2a0b45a8224e`, built by staging **only** the canonical artifact, `git stash create`, then resetting the index. Worktree artifact remains **unstaged**; no branch, commit-on-branch, tag, or release.
- change_ref: `fd44f4fdf4a2b0c1e5e70f8369bd2a0b45a8224e`
- reconcile:  Director verified the live output equals the writer's reconstructed §8 numbers **bit-for-bit** — self-test 16/16; agents 7/20/9/11/9 = 56; 22 core-provided; 9 orphans; pack-owned 11/3/2/3; the 9-name orphan set matches artifact §4.3 / §5 exactly. No discrepancy; the reconstruction was faithful. The on-disk canonical artifact is byte-identical to the content bound at `change_ref` (operator staged that exact file before `stash create`).
- effect:     `change_ref` recorded; `Q-pack-split-partition-lock-01` cleared from `waiting_on_questions`; item unblocked `blocked -> in-review`; the single required `independent-architecture` review is dispatched next.

## HANDOFF 2026-08-24-1938 — director-chief-of-staff -> principal-swe-architect

- did:       Operator answered blocking action `Q-pack-split-partition-lock-01`; reconciled live `pack-preview.mjs` output against the writer's reconstructed §8 numbers (exact match). Recorded `change_ref: fd44f4fdf4a2b0c1e5e70f8369bd2a0b45a8224e` (dangling `git stash create` review-binding object; on-disk artifact byte-identical to that ref; worktree unstaged; no branch/commit/tag/release). Cleared `waiting_on_questions`, moved `blocked -> in-review` (`resume_state: null`), set `next_role: principal-swe-architect`. **Reserved the item serially as sole grantor** for the required review: lease holder `principal-swe-architect`, token `psa-2026-08-24-1938-rv71`, `version_at_grant: 5`; version 5 -> 6; re-read to confirm the grant.
- state:     in-review
- needs:     `principal-swe-architect` performs the item's **single required** `independent-architecture` review, binding to **exactly** `change_ref fd44f4fdf4a2b0c1e5e70f8369bd2a0b45a8224e` (== the on-disk canonical artifact `kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md`). **Ratify or reject each, with reasons:** (1) the §5 orphan dispositions — 8 moves (`personal` +4 demo toolchain `create-product-demo`/`demo-capture`/`demo-narrate`/`demo-zoom`; `engineering` +`onboard-to-codebase` +3 review lenses `review-dependencies`/`review-performance-scale`/`review-success-metrics`) and 1 keep-in-core (`fleet-observation`); (2) whether **all-three-firing-path** coverage (inheritance / direct `/skills` invoke / skill-to-skill composition) is correctly deferred to `dependency-guarantees` vs. under-specified in the lock; (3) the **separate `kai-core` identity + uninstall-first / coexistence-forbidden** model (§6/§7 + northstar non-negotiables); (4) the **generate-not-move** root-as-single-source-of-truth model; (5) the **non-markdown asset ownership boundary** (demo `scripts/*.mjs`, audio) and its §9 routing; (6) **hooks fire exactly once** handling; (7) the §9 **downstream gate routing** to the correct owning items/milestones; and confirm §7 **dependency-direction** (core → nothing, no pack → pack, shared skills resolve from core across the boundary). The operator-captured live `pack-preview.mjs` output is in the ANSWER above and in item Evidence — treat it as the ratifying live run; if you have a shell, independently re-run the two commands and say so, otherwise render the architecture verdict on the artifact content + the recorded run and state your evidence basis (do **not** fabricate a live run). Record a verdict in `completed_reviews` (`role: principal-swe-architect`, `kind: independent-architecture`, `change_ref`, verdict, timestamp), append a HANDOFF, verify `holder`/`token`/`version` before each state-changing write, and stop with a `COLLISION` record if they differ. **Do not** edit the artifact, create branches/commits/PRs, merge/tag/release, or run external host gates. If ratification finds the **partition itself** must change, stop and raise it to the steward (`principal-product-manager`) as a question rather than editing downstream.
- artifacts: kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md (== `change_ref fd44f4f…`); kai/coordination/items/pack-split-partition-lock.md (version 6, `in-review`, leased to `principal-swe-architect`)
- evidence:  operator-run live `pack-preview.mjs` output (ANSWER 1938); on-disk roster (56 `agents/*.agent.md`, 50 `skills/*/SKILL.md`); `scripts/pack-preview.mjs` (`PACKS` + `planPacks()` + `selfTest()`); `kai/initiatives/pack-split/northstar.md` (non-negotiables + milestone `required_items`); `docs/proposals/pack-architecture.md` (measured host findings)
- questions: none blocking
- next:      `principal-swe-architect` returns a ratify/reject verdict bound to `change_ref`. On **clean** ratification: director returns the item to `principal-product-manager` (owning role) to verify acceptance and move it to `completed`, then updates the `partition-lock` milestone, `deliverables.md`, `BOARD.md`, `ACTIVE.md`, and the initiative log, and evaluates promotion of the gated `pack-split-engineering-decomposition`. On **reject**: director routes findings to `principal-technical-writer` to revise the artifact, re-mints `change_ref` (operator, no shell in-host), and re-runs the review.

## HANDOFF 2026-08-24-1947 — principal-swe-architect -> director-chief-of-staff

- verdict:   **RATIFIED** — bound to `change_ref fd44f4fdf4a2b0c1e5e70f8369bd2a0b45a8224e` (== the on-disk canonical artifact, operator-confirmed byte-identical). No partition change required; nothing escalated to the steward.
- did:       Performed the item's single required `independent-architecture` review. Rather than trust the script output, I **independently re-derived the whole partition by hand** — read all 56 `agents/*.agent.md` `**Inherits:**` lines and all 50 `skills/*/SKILL.md` dirs, replayed the `planPacks()` rule myself, and recomputed the provider of every skill. My hand-replay reproduces the artifact and the operator's live run **exactly**: 5 disjoint packs 7/20/9/11/9 = 56 (0 unassigned, 0 double-claimed); 22 core-provided (15 core-shared + 7 core-internal) + 19 pack-owned (eng 11 / prod 3 / gtm 2 / personal 3) + 9 orphan = 50; the 9 orphans sit on zero Inherits lines. Checked each §5 disposition against the invoking agent/skill bodies and each §7 claim against the roster.
- evidence basis: **No executable shell in this host** — my toolset is read/search/edit only, consistent with the three prior `NO_SHELL_AVAILABLE` dispatches; I did **not** run `node`/`git` and do **not** label anything a live run by me. Basis = (a) an independent by-hand re-derivation of `planPacks()` over the on-disk roster [my own computation, not the script's], matching (b) the operator-captured live `pack-preview.mjs` run in ANSWER-1938 (self-test 16/16; 7/20/9/11/9 = 56; 22 core +9 orphan; 11/3/2/3) bit-for-bit. The hand-replay is a stronger independent check than a script re-run because it does not trust the computation under test.
- findings (per point):
  - **(1) §5 orphan dispositions — RATIFIED 9/9.** Rule applied correctly. Demo x4 → `personal`: `creative-video-director` (personal) names them in its body (create-product-demo L207; demo-capture/zoom/narrate L199; demo-narrate L92/118/123/274); no consumer outside personal. `onboard-to-codebase` → `engineering`: `user-invocable` engineering-craft repo-mapping report, catalogued under *Engineering craft*. 3 review lenses → `engineering`: named in `workflow-doc-review` (engineering) body (L95/97/98), siblings of the 6 engineering-owned lenses. `fleet-observation` → **keep in `core`**: I read the skill — it observes the whole kai subagent fleet (cross-department participation over `.kai/observed.jsonl`/`activity.jsonl`), genuinely cross-cutting and meaningful with no department installed. Architecturally the dispositions are **firing-path-aware**: every orphan lands **with its sole caller** (or in always-present core for the operator-invoked one), creating **no new cross-boundary composition edges** — the safest placement available.
  - **Caveat 1a (`fleet-observation` prefix) — non-blocking, downstream, and now MANDATORY not optional.** Keeping it in core makes it the one core-provided skill without the `kai-core-*` prefix, breaking the northstar non-negotiable. The **placement (core) is correct and ratified**; the fix is a rename to `kai-core-fleet-observation` — a behaviour change correctly out of scope for this docs-only lock, and **forced** by `dependency-guarantees` acceptance ("core skills carry the `kai-core-*` prefix"): the CI prefix check will fail on it until renamed, so it self-enforces. Carry as a required action into `dependency-guarantees`; it is **not** a partition defect.
  - **Caveat 1b (3 review lenses vs Inherits line) — non-blocking, downstream.** `workflow-doc-review` names them in body but not on `**Inherits:**`. Pack owner (engineering) is stable either way. Whether to bind them on the Inherits line (inherited vs composed firing path) is an engineering-content call for the decomposition; it changes the path, not the owner.
  - **(2) All-three-firing-paths — deferral to `dependency-guarantees` is SOUND; the lock does not under-specify placement.** Placement is decided now; cross-boundary resolution for direct-`/skills` and skill-to-skill composition is a measured host guarantee of the same class as Finding 2 (which measured only the inheritance path) and belongs with the preflight/compat + cross-pack validator. Because §5 co-locates every orphan with its caller, the deferred work has **less** to prove, not more. Ratified.
  - **(3) Separate `kai-core` identity + uninstall-first / coexistence-forbidden — RATIFIED as recorded.** The distinct `kai-core-*` namespace is the correct defence against host Finding 6 (same-named skills from two plugins are both exposed, namespace-qualified; Phase-1 D/D2 showed load-order decided the winner) — a legacy unprefixed `kai:team-operating-rules` cannot satisfy a pack agent's `kai-core-team-operating-rules` inheritance. The lock correctly **records** the identity/collision model and **routes** the uninstall-first/coexistence-refusal mechanism to `pack-split-migration-doctor` (first-pack-extracted). Sound.
  - **(4) Generate-not-move source-of-truth — RATIFIED, unambiguous.** Root `agents/`+`skills/` stay authoritative; `pack-preview.mjs` reads root and writes only a throwaway `--out` tree (evidence, never a deliverable); the lock moves/changes nothing (§1). Matches the northstar non-negotiable exactly.
  - **(5) Non-markdown asset boundary — boundary correctly drawn; deferral right.** The partition scopes to the host-loadable surface (agents + skills `plugin.json` loads); scripts/audio are runtime-invoked assets (confirmed: `demo-capture` shells `scripts/demo-capture.mjs`/`demo-zoom.mjs`), so placement is a generator concern. The rule is mechanical and forced — an asset travels with the sole skill that invokes it (demo scripts → personal; `scripts/observe-*.mjs` → core with `fleet-observation`); any asset invoked across >1 pack promotes to core, mirroring `planPacks()`. No genuine ambiguity to decide **now**; recommend the decomposition state that rule explicitly in `pack-split-generated-pack-trees`. Routing to generated-pack-trees + `pack-split-crosspack-validator` (script-path validity across the boundary) is correct.
  - **(6) Hooks fire exactly once — deferral to `dependency-guarantees` correct.** Hooks aren't agents/skills → out of the partition's scope; multi-plugin hook duplication is a composition-correctness hazard best CI-enforced on the `--all` preview where the guarantee work lives. Natural resolution exists (hooks ride with core — the observe hook's only consumer `fleet-observation` is core), and `dependency-guarantees` precedes `first-pack-extracted`, so it's caught before the first real compose. Ratified.
  - **(7) §9 downstream gate routing — correct and complete for the deferred concerns.** Cross-checked every row against the northstar milestone `required_items`: the four `dependency-guarantees` items (preflight-compat, degraded-refusal, crosspack-validator, ci-partition-checks) and the three `first-pack-extracted` items (generated-pack-trees, migration-doctor, host-gates) all map. Nothing mis-routed or silently dropped. The table lists *deferred cross-cutting concerns*, not every downstream item, so primary deliverables (`pack-split-first-department`, `-onboarding-installer`) are legitimately absent — they are milestone deliverables, not concerns this lock raises.
  - **§7 dependency-direction — CONFIRMED (all three).** (1) Core depends on nothing: every skill on all 7 core agents' Inherits lines is `kai-core-*` (0 pack/orphan skills reached) — provable now, verified. (2) No pack→pack: each of the 19 pack-local skills is inherited by exactly one pack, and any multi-pack skill is promoted to core by `planPacks()`, so the inter-department graph is empty — provable now, verified. (3) Shared skills resolve from core across the boundary: the 15 core-shared skills are measured (Finding 2) as cross-plugin-resolvable; making it fail-closed + CI-enforced is correctly downstream. Direction holds: `department → kai-core`, never `pack → pack` or `core → pack`.
- state:     in-review (unchanged — I do **not** move it to `completed`; the owning role `principal-product-manager` does). Lease cleared to null; version 6 -> 7; `next_role: director-chief-of-staff`. No artifact/agent/skill/script/`plugin.json` edit; no branch/commit/PR/tag/release; no host gates run.
- needs:     Director returns the item to `principal-product-manager` (owning role) to verify acceptance and move it to `completed`, then update the `partition-lock` milestone, `deliverables.md`, `BOARD.md`, `ACTIVE.md`, and the initiative log, and evaluate promoting the gated `pack-split-engineering-decomposition`. **Carry three non-blocking downstream actions forward** (none blocks closure): (a) rename `fleet-observation` → `kai-core-fleet-observation` under `dependency-guarantees` (forced by its CI prefix check); (b) decide whether to bind the 3 orphan review lenses on `workflow-doc-review`'s Inherits line; (c) adopt the explicit asset-ownership rule (assets follow the sole invoking skill's pack; multi-pack assets promote to core) in `pack-split-generated-pack-trees`.
- artifacts: kai/coordination/items/pack-split-partition-lock.md (version 7, `in-review`, lease cleared, verdict in `completed_reviews`); kai/coordination/threads/pack-split-partition-lock.md
- evidence:  independent by-hand re-derivation of `planPacks()` over 56 Inherits lines + 50 skill dirs (matches operator ANSWER-1938 live run bit-for-bit); §5 dispositions checked against `creative-video-director`/`workflow-doc-review` bodies + `onboard-to-codebase`/`create-product-demo`/`fleet-observation` frontmatter; §7 against the roster; §9 against northstar milestone `required_items`; `generate-catalog.mjs` CATEGORIES (confirms `fleet-observation` unprefixed + the review-lens grouping). No shell executed.
- questions: none blocking.
- next:      director-chief-of-staff — return to `principal-product-manager` for acceptance/closure; no re-mint needed (clean ratification).

## HANDOFF 2026-08-24-1959 — principal-product-manager -> director-chief-of-staff

- verdict:   **ACCEPTED — item closed `completed`.** As owning role and steward I verified acceptance against the evidence (not a rubber-stamp): all five acceptance criteria are met on the artifact at the `artifact_target`; the single required `independent-architecture` review is RATIFIED and bound to the exact `change_ref fd44f4fdf4a2b0c1e5e70f8369bd2a0b45a8224e` recorded on the item; and the operator's live `pack-preview.mjs` run (ANSWER-1938) matches the recorded partition bit-for-bit.
- did:       Independently re-checked the partition arithmetic and the review binding before accepting: agents 7/20/9/11/9 = 56 (§3, §3.6 roster diff — 0 unassigned, 0 double-claimed); skills 22 core-provided (15 core-shared + 7 core-internal) + 19 pack-owned + 9 orphan = 50 (§4); orphan dispositions keep 1 (`fleet-observation`) + move 8 (`personal` +4 demo, `engineering` +4 = `onboard-to-codebase` + 3 review lenses) = 9, ratified 9/9 (§5); naming decision — `kai-core-*` prefix, one version-pin `kai-core-contract-v1`, no taxonomy segment (§6); docs-only, no agent/skill/script/`plugin.json` change (§1). Confirmed `completed_reviews` holds `principal-swe-architect`/`independent-architecture`, `change_ref fd44f4f…`, `verdict: ratified` == the item `change_ref`. No acceptance gap. Closed the item: `state in-review -> completed`, lease cleared to null, `next_role: director-chief-of-staff`, `version 8 -> 9`.
- state:     completed
- needs:     Director refreshes the derived indexes (`BOARD.md`, `ACTIVE.md`) to reflect closure and the new focus, and dispatches the now-unblocked `pack-split-engineering-decomposition` (promoted to `ready` this pass) to `principal-swe-manager`. No re-mint, no further review — closure is clean.
- artifacts: kai/coordination/items/pack-split-partition-lock.md (version 9, `completed`, lease null); kai/initiatives/pack-split/artifacts/docs/pack-split-partition-lock.md (accepted deliverable, promoted in deliverables.md); kai/initiatives/pack-split/deliverables.md; kai/initiatives/pack-split/log.md; kai/initiatives/pack-split/northstar.md (`scope.current: partition-lock -> dependency-guarantees`); kai/coordination/items/pack-split-engineering-decomposition.md (version 2, `proposed -> ready`)
- evidence:  five acceptance criteria checked on the artifact; ratified `independent-architecture` review bound to `change_ref fd44f4fdf4a2b0c1e5e70f8369bd2a0b45a8224e`; operator live run ANSWER-1938 == §8 bit-for-bit; architect hand re-derivation matches — captured 2026-08-24 from C:\src\kai
- decision:  **Deliverable promoted** — the locked partition doc is recorded in `deliverables.md` (milestone `partition-lock`, source item `pack-split-partition-lock`, stays an initiative artifact, status completed/accepted). **Milestone `partition-lock` satisfied** — its one required item reached `completed`. **`scope.current` advanced** `partition-lock -> dependency-guarantees` (the decomposition pass is the first action there). **Gated promotion made** — `pack-split-engineering-decomposition` `proposed -> ready` (dependency genuinely satisfied), `next_role: principal-swe-manager`, priority 10, with the three non-blocking architect caveats carried into its scope/notes.
- caveats carried (non-blocking, into decomposition): (a) rename `fleet-observation` -> `kai-core-fleet-observation` under `dependency-guarantees` (forced — its CI prefix check fails until renamed); (b) decide whether to bind the 3 review lenses (`review-dependencies`/`review-performance-scale`/`review-success-metrics`) on `workflow-doc-review`'s `**Inherits:**` line (firing path, not owner); (c) adopt the explicit asset-ownership rule (a non-markdown asset travels with the sole skill that invokes it; any asset invoked across >1 pack promotes to core) in `pack-split-generated-pack-trees`.
- questions: none
- next:      director-chief-of-staff — refresh `BOARD.md`/`ACTIVE.md` and dispatch `pack-split-engineering-decomposition` (now `ready`) to `principal-swe-manager`; hold no other pack-split items (the downstream records don't exist until the decomposition pass creates them).
