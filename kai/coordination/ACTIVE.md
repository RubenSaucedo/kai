# Active initiatives

Operational focus pointer, not the permanent record. Each row names an
initiative `slug` currently receiving attention and, briefly, why. This file
lists only the current focus — `kai/initiatives/INDEX.md` is the permanent
all-status catalog, and removing a terminal initiative from this file must
never make it undiscoverable there.

| slug | why active |
|------|------------|
| pack-split | `partition-lock` **completed**; `pack-split-engineering-decomposition` **completed** — steward-accepted 14-item build & ship decomposition (12 required IDs + 2 manager splits) at `kai/initiatives/pack-split/artifacts/decisions/pack-split-engineering-decomposition.md`. Focus is `dependency-guarantees`. The single first implementation item **`pack-split-generator-gates`** is **`ready`** (priority 10, `next_role: principal-swe-infra`, dependency met) for `director-chief-of-staff` to dispatch; `pack-split-host-semantics-spike` recommended for parallel operator dispatch. The other 12 records stay `proposed` for milestone-by-milestone grooming. Non-blocking: 4 open questions (review-lens binding routed to `principal-swe-architect`, `packs/` tree location, first-department `shipped` semantics, director-availability completeness). |

The pack-split initiative is the current focus.
