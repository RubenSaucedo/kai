# Active initiatives

Operational focus pointer, not the permanent record. Terminal initiatives remain
discoverable in `kai/initiatives/INDEX.md`.

**Current snapshot — 2026-08-27-1745.** `pack-split` remains active in
`five-pack-split-shipped`, now **open at 7 of 8** typed required items.
`pack-split-release-12c-3-engineering` is truthfully `shipped` as public
`v1.0.3`, with operator merge, successful final-head and exact-main CI,
isolated four-pack marketplace verification, annotated tag, and release
evidenced in its canonical ship record.

`pack-split-release-12c-4-gtm` is `in-review` at exact implementation ref
`1ad873725e62f53efd0c0005edd897e1672c915b`. Independent SRE and architecture
reviews are both **APPROVED**, P0/P1/P2 = 0/0/0, at that same ref. It routes
to `workflow-ship` for fresh final-head CI, operator merge, live probe, tag,
and release. No GTM publication, milestone closure, or initiative closure is
claimed.

| slug | why active |
|------|------------|
| pack-split | Engineering is live at `1.0.3`; GTM `1.0.4` has both exact-ref reviews approved and awaits workflow-ship. Active milestone `five-pack-split-shipped` is open at 7/8. |
