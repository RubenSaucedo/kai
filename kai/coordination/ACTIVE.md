# Active initiatives

Operational focus pointer, not the permanent record. Terminal initiatives remain
discoverable in `kai/initiatives/INDEX.md`.

**Current snapshot — 2026-08-27-1716.** `pack-split` remains active in
`five-pack-split-shipped`, now **open at 6 of 8** typed required items.
`pack-split-release-12c-1-hardening` and
`pack-split-release-12c-2-product` are `shipped`; the review-lens decision is
`completed`.

`pack-split-release-12c-3-engineering` is `in-review` at exact implementation
ref `27804defe2f5f7fa16c2f5373884691203d21974`, routed first to
`principal-sre`; the already-declared architecture review is independently
required at the same ref. The settled architecture is unchanged: the three
review lenses remain runtime-dispatched. The implementation records zero diff
to the canonical `workflow-doc-review` body and the three named
`SKILL_OWNER_OVERRIDES` entries relative to pre-implementation main.

`pack-split-release-12c-4-gtm` remains `proposed` behind engineering
`shipped`; it is not promoted. No milestone or initiative closure is claimed.

| slug | why active |
|------|------------|
| pack-split | Product is live at `1.0.2`; engineering `1.0.3` is in independent SRE and architecture review at `27804def…`; GTM remains proposed. Active milestone `five-pack-split-shipped` is open at 6/8. |
