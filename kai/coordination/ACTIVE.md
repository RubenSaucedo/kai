# Active initiatives

Operational focus pointer, not the permanent record. Terminal initiatives remain
discoverable in `kai/initiatives/INDEX.md`.

**Current snapshot — 2026-08-27-1736.** `pack-split` remains active in
`five-pack-split-shipped`, now **open at 7 of 8** typed required items.
`pack-split-release-12c-3-engineering` is truthfully `shipped` as public
`v1.0.3`, with operator merge, successful final-head and exact-main CI,
isolated four-pack marketplace verification, annotated tag, and release
evidenced in its canonical ship record.

`pack-split-release-12c-4-gtm` is the **sole ready next release item** at
priority 50, routed to `principal-swe-infra`. Its existing final-publish scope
and `1.0.4` version are unchanged. No GTM implementation, milestone closure, or
initiative closure is claimed.

| slug | why active |
|------|------------|
| pack-split | Engineering is live at `1.0.3`; GTM `1.0.4` is the sole ready next release item. Active milestone `five-pack-split-shipped` is open at 7/8. |
