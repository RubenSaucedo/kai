This expands scope; do not fold it into current screen work. Treat it as a separate proposal.

PROPOSAL
  problem: Search Results has no way to compare similar results in one place, so operators open multiple tabs and lose side-by-side context.
  proposed_change: Add a persistent comparison tray on the Search Results page that lets operators pin up to three results and compare them side by side.
  friction_cost: Adds pin/unpin controls in each row, a new pinned-results surface, tray state management, and a new side-by-side comparison interaction.
  mission_tradeoff: Improves comparison efficiency, but it adds a new surface and workflow step, so it expands the current screen beyond its existing filter/sort/save/open behavior.
  scope_target: Search Results follow-on / dedicated compare workflow proposal.
