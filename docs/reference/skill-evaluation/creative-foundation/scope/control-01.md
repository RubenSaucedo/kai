This expands scope, so do not fold it into the current screen work.

Next step today: treat it as a separate inline proposal and keep the current Search Results page work limited to the existing filter/sort/save/open flows.

Inline proposal payload:

“Add a persistent comparison tray to Search Results that lets operators pin up to three results and compare them side by side without leaving the page. This solves the current tab-sprawl problem when reviewing similar matches. Scope includes pin/unpin controls, tray state management, and a side-by-side comparison view. Please confirm whether to proceed as a separate UX/product proposal.”
