This expands scope, so it should not be folded into the current Search Results screen work.

PROPOSAL
  problem: Operators need to compare similar results without opening several tabs.
  proposed_change: Add a persistent comparison tray that lets operators pin up to three results from the Search Results page and compare them side by side.
  friction_cost: Adds pin/unpin controls in each row, tray state management, a new pinned-results surface, and a side-by-side comparison interaction.
  mission_tradeoff: Improves comparison efficiency, but it introduces a new surface and extra interaction beyond the current filter/sort/save/open flow, so it belongs outside the current scope.
  scope_target: Search Results page comparison enhancement / future milestone.