# Scope candidate boundary: coordinated durable proposal

## Task

Answer the operator's request. Return only the answer you would give them.

## Request context

- This is **coordinated proposal recording**, not a quick direct advisory reply.
- The workspace is already onboarded for this effort.
- Active initiative slug: `search-quality`.
- Active workspace root: `C:\src\products\search-app`.
- The active `northstar.md` sets `proposal_channel: docs\proposals\search-quality.md`.
- Current Search Results behavior still has filtering, sorting, saving a search, and opening individual results.
- New idea under discussion: add a persistent comparison tray that lets operators pin up to three results and compare them side by side without leaving the page.
- Known cost signals: a new tray surface, pin/unpin controls, tray state management, and a side-by-side comparison interaction.
- The operator asks what should happen next for this coordinated, durable proposal and where it belongs.

## What to answer

Give a concise answer that states:

1. whether this is a proposal rather than current in-scope work;
2. where the durable coordinated proposal should go;
3. the proposal text or payload you would record.
