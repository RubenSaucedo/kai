That expands scope, so I’d treat it as an inline proposal rather than folding it into the current screen work.

Next step: capture it as a separate proposal for the Search Results page.

Proposal: “Add a persistent comparison tray to the Search Results page that lets operators pin up to three results and compare them side by side without leaving the page. Include pin/unpin controls in result rows, tray state management, and a side-by-side comparison view.”
