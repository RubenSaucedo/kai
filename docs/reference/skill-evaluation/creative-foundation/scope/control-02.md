This expands scope. Do not fold it into the current screen work.

Use it as an inline proposal today:

“Add a persistent comparison tray to the Search Results page that lets operators pin up to three results from the list and compare them side by side without leaving the page.”
