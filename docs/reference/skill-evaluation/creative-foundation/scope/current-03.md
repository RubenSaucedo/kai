Expands scope. This is a new comparison workflow, not a refinement of the current Search Results page, so I would not fold it into the current screen work.

PROPOSAL
  problem: Operators can’t easily compare similar results without opening several tabs.
  proposed_change: Add a persistent comparison tray that lets them pin up to three results and compare them side by side on the Search Results page.
  friction_cost: Adds pin/unpin controls, tray state management, a new persistent surface, and a side-by-side comparison interaction.
  mission_tradeoff: It would improve comparison speed, but it introduces a new surface and extra complexity, so it should stay out of the current scope.
  scope_target: Follow-on Search Results comparison workflow proposal.
