# Scope final analysis

**Method:** `kai-core-scope-discipline`  
**Candidate guide identity:** SHA-256 `d59d79b79687dd640f6650eff89b793c9ed61ed6414d26b2ad04e8c83dfbc22f`  
**Baseline current guide freeze:** commit `db386da78e857d2c863ae373c9670efb6f67e410`  
**Committed candidate containing the frozen content:** `603e3324386466d2edd8829c41c0feadab1bdfeb`  
**Model/config:** `gpt-5.4-mini`, `general-purpose`, reasoning effort `medium`

## Baseline status carried forward

The baseline remains **nondiscriminating**. On the fixed direct unonboarded
comparison-tray case, both the control arm and the unchanged-guide arm returned
useful inline proposal text without forcing workspace onboarding, backlog
creation, or proposal-channel recording first.

That baseline supports **no efficacy claim**.

## Candidate case result

The five candidate samples on the unchanged direct case were all non-failing.
All five:

1. classified the comparison tray as scope-expanding;
2. refused to fold it into current screen work;
3. returned an unadopted inline `PROPOSAL` block;
4. did not authorize implementation;
5. grounded the answer in the supplied new-surface and workflow costs.

## Candidate-only boundary results

### Boundary 1: coordinated durable proposal

`candidate-boundary-durable.md` treated the idea as a proposal rather than
current in-scope work and routed the durable coordinated record to the
authorized workspace path `C:\src\products\search-app\docs\proposals\search-quality.md`.

### Boundary 2: direct proposal without implementation authority

`candidate-boundary-direct.md` said the comparison tray expands scope and that a
direct proposal is **not** enough to start building it as part of the current
screen work.

## Interpretation

No substantive candidate failure appeared in these seven candidate-side outputs.
The sampled behavior matched the intended seam: direct advisory work can return
an unadopted proposal inline, while coordinated durable proposal recording still
uses the authorized workspace/proposal channel.

Because the baseline case was nondiscriminating, this final evidence supports
**no measured efficacy claim** over the old wording. The supported conclusion is
narrower: **the candidate did not introduce sampled regression, and the
candidate-only boundaries aligned with the intended contract in these samples.**

## Preserved protocol deviation

The earlier unchanged-guide miswrite to
`docs\reference\skill-evaluation\creative-foundation\scope\current\current-02.md`
remains part of the record. It is a sample-protocol deviation, not a
scope-policy behavior failure.

## Limits

- Ambient harness/core context remained a confound.
- Candidate-only boundaries are consistency checks, not baseline comparisons.
- No host acceptance or implementation behavior is established here.
