This expands scope, so I would not fold it into the current Search Results work. The useful next step today is to capture it as a proposal and keep the current screen focused on filtering, sorting, saving, and opening results.

PROPOSAL
  problem:          Operators cannot easily compare several similar search results without opening multiple tabs.
  proposed_change:  Add a persistent comparison tray that lets operators pin up to three results and compare them side by side on the Search Results page.
  friction_cost:    Adds a new pinned-results surface, pin/unpin controls in each row, tray state management, and a new side-by-side comparison interaction.
  mission_tradeoff: This improves review efficiency, but it introduces an extra UI surface and workflow step, so it should not be absorbed into the current screen by default.
  scope_target:     Search Results page enhancement / bulk review comparison workflow.
