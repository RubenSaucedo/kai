This expands scope, so I would not fold it into the current screen work. The right next step is to keep the Search Results page focused on filtering, sorting, saving, and opening results, and treat comparison as an unadopted proposal.

PROPOSAL
  problem: Operators need a faster way to compare similar search results without opening many tabs.
  proposed_change: Add a persistent comparison tray that lets operators pin up to three results from the Search Results page and compare them side by side.
  friction_cost: Adds a new pinned-results surface, pin and unpin controls in each result row, tray state management, and a new side-by-side comparison interaction.
  mission_tradeoff: It improves review efficiency, but it introduces a new capability and UI surface, so it should stay out of current screen work unless explicitly approved.
  scope_target: Search Results page follow-on proposal / comparison workflow enhancement.
