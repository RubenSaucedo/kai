This is a proposal, not current in-scope work. Record it in `docs\proposals\search-quality.md` under `C:\src\products\search-app`.

PROPOSAL
  problem: A persistent comparison tray for pinning up to three results and comparing them side by side is not part of the current Search Results behavior.
  proposed_change: Add a tray surface with pin/unpin controls, tray state management, and a side-by-side comparison view without leaving the page.
  friction_cost: Adds a new tray surface, new pin/unpin actions, persistent tray state, and an additional comparison interaction.
  mission_tradeoff: Improves result evaluation, but it adds a new surface and more user steps, so it expands scope instead of refining existing search behavior.
  scope_target: `docs\proposals\search-quality.md`
