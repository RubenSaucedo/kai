This expands scope, so do not fold it into the current Search Results page work.

Next step for today: keep the active screen focused on the existing list/filter/sort/save/open flows, and capture the comparison tray as a separate proposal.

Inline proposal payload:
“Add a persistent comparison tray for Search Results that lets operators pin up to 3 results and compare them side by side without leaving the page. This is a new surface and interaction, so it should be planned separately from the current screen work.”
