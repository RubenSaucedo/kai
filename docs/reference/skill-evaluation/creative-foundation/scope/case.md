# Scope baseline case

## Task

Answer the operator's direct advisory request. Return only the answer you would give them.

## Request context

- This is a quick direct product/design advisory conversation before any coordinated implementation work starts.
- The repository is **not onboarded for this work**: no active initiative, no loaded `northstar.md`, no workspace proposal channel, and no backlog artifact are attached to this request.
- The operator wants a useful inline answer they can react to today.
- Current surface: the desktop Search Results page already supports filtering, sorting, saving a search, and opening individual results.
- Current behavior: there is no compare mode, no side-by-side comparison tray, and no multi-result bulk review workflow.
- Proposed idea raised in conversation: add a persistent comparison tray that lets operators pin up to three results from the list and compare them side by side without leaving the page.
- Known value: operators sometimes open several similar results in separate tabs because it is hard to compare details across matches.
- Known cost signals: this would add a new pinned-results surface, pin/unpin controls in each result row, tray state management, and a new side-by-side comparison interaction.
- No one has approved implementation. The operator is asking whether this should be treated as an inline proposal or as something that should just be folded into the current screen work.

## What to answer

Give a concise answer that states:

1. whether the idea stays in current scope or expands scope;
2. the useful next-step answer for this direct advisory conversation today;
3. if it is a proposal, the exact inline proposal payload or equivalent concise proposal text you would give the operator now.
