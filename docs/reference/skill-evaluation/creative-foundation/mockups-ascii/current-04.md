No mockup or options exercise is needed here.

Recommended action: make the copy-only change directly by renaming the secondary button from `Create alert` to `Save search` and leaving the toolbar layout, spacing, and interaction flow unchanged.

Caveat: the supplied evidence already says `Save search` fits at `1280px`, so there is no expected truncation or layout risk to resolve.
