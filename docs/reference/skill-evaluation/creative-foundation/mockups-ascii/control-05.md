No mockup or options exercise is needed here. This is a routine copy-only change with no layout, interaction, or responsive-design decision open.

Proceed with the bounded implementation step: rename the secondary button from `Create alert` to `Save search` and keep the existing toolbar structure unchanged.
