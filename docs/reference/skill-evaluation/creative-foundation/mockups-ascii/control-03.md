No mockup or options exercise is needed here. This is a routine copy-only change with no layout, hierarchy, interaction, or responsive design decision to resolve.

Proceed directly with the label update from `Create alert` to `Save search` in implementation planning, and keep the existing toolbar structure unchanged.
