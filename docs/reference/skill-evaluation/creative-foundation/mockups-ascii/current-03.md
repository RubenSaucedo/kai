No mockup or options exercise is needed here. This is a bounded copy-only change, so the right action is to update the secondary button label from `Create alert` to `Save search` and keep the existing layout, spacing, and interaction flow unchanged.

The only caveat is that the supplied evidence already shows `Save search` fits at `1280px`, so there is no layout-risk reason to widen scope.
