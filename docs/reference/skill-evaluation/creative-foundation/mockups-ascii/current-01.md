No mockup or options exercise is needed here. This is a routine copy fix, not a layout or interaction decision.

Proceed directly to implementation: rename the secondary button from `Create alert` to `Save search` and keep the existing toolbar layout unchanged.

The only caveat is to preserve the current button width, but the supplied evidence already says `Save search` fits at `1280px` with no truncation or crowding.
