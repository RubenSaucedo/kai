No mockup/options exercise is needed. This is a copy-only change with a fixed toolbar layout, and the supplied evidence says `Save search` already fits at 1280px.

Recommended action: change the secondary button label from `Create alert` to `Save search` and proceed directly to implementation planning.