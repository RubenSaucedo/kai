# Mockups ASCII candidate boundary: explicit single mock

## Task

Answer the operator's request. Return only the answer you would give them.

## Request context

- The operator explicitly wants **one ASCII mock**, not multiple options.
- Surface in scope: desktop Search Results page, fixed toolbar container at `1280px`.
- What stays fixed:
  - the toolbar remains a two-row region;
  - the top row already contains `SearchField`, `SortSelect`, and `Save search`;
  - the filter area stays directly beneath that row, left-aligned to the search field;
  - no browser, HTML file, or design-system artifact is needed for this request.
- Supplied current surface facts:
  - the main row is already decided;
  - the second row needs labels for six visible active filter chips;
  - chip text is schematic only; exact spacing and box dimensions do not need to be precise;
  - this is a structural communication request, not a styling or interaction decision.

## What to answer

Give:

1. a brief sentence naming the structural question and what stays fixed;
2. one ASCII mock of the two-row toolbar arrangement;
3. one short note about the relevant trade-off or limitation of the sketch.
