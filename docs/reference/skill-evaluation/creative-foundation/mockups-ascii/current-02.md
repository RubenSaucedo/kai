No mockup or options exercise is needed here.

1. Update the secondary button label from `Create alert` to `Save search`.
2. Keep the toolbar layout, spacing, and interaction flow unchanged.
3. Use the supplied width evidence as-is; no layout risk is indicated at `1280px`.

Only caveat: if the label changes again or localization lengthens it, recheck fit before shipping.
