No mockup/options exercise is needed here. The layout, spacing, and interaction flow stay the same, and the supplied evidence says `Save search` already fits at 1280px.

Recommended action:
- Rename the secondary button from `Create alert` to `Save search`.
- Keep the toolbar structure and sizing unchanged.
