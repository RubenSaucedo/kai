No mockup or options exercise is needed here. This is a routine copy correction, and the toolbar layout, spacing, and interactions stay fixed. The bounded action is to rename the secondary button from `Create alert` to `Save search` and proceed.

The only caveat is that the supplied evidence already says `Save search` fits at 1280px, so there is no layout or truncation risk to resolve before implementation.
