Refresh `design-system.md` from the supplied evidence.

- The evidence basis is sufficient because it covers the full in-scope authenticated desktop app for this branch: Dashboard, Search Results, Saved Searches, Reports, and Settings, with the existing default, empty, loading, and error states at desktop widths.
- Treat the suggested `--space-5` gap as a proposal, not current app fact. Keep it labeled `proposed` until it is verified against implementation truth.
- Do not promote that token into the design system until frontend feasibility review has happened and the change is independently accepted.
