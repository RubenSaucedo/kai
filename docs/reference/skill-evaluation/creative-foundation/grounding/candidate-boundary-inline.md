Use the current spacing system as-is for this screen today.

- Keep `EmptyStateCard` on `--space-4` for the body copy and primary action.
- Do not treat a `6px` eyebrow-to-title gap as current fact; it is only an unverified idea.
- Frame the `6px` gap as a proposed refinement, not a settled token or adopted scale step.
- If it still seems worthwhile, ask for FE feasibility review and a token decision before using it anywhere.
