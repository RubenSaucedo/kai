Recommended direction: move the active filter chips to a dedicated second row beneath the main toolbar controls, left-aligned with the search field, with a small vertical gap.

Why:
- At 1280px, keeping six chips on the main row pushes Save search out of view.
- There is no supplied overflow pattern for chips.
- The app already uses a second row for filters on another toolbar when horizontal room is tight.
- Visible active filters matter more than preserving a single row.

Sketch:

| [ Search field ................................ ] [ Sort ] [ Save search ] |
| [ Chip 1 × ] [ Chip 2 × ] [ Chip 3 × ] [ Chip 4 × ] [ Chip 5 × ] [ Chip 6 × ] |

Follow-up before adoption: none required from the supplied evidence.
