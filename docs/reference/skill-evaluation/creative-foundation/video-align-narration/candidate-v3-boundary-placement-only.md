Placement can proceed from the existing inputs.

Bounded fit result:
- All referenced states, including the interior gate at `start_after: edit-name`, are marked `ok` and reported as visible/readable.
- The narration may start after `edit-name` ends at 7.4s.
- The available window is 7.4s to 15.0s, so the 4.2s clip fits with 3.4s of slack.

Exact next action:
- Prepare the placement plan from the supplied screenplay, measured take, and measured narration take using the existing clip as-is.
