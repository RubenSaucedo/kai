No placement yet: the narration cannot start after `edit-name` because that step is unsettled, and the skill explicitly says an unsettled relevant state blocks composition.

Exact next action: get a new measured take where `edit-name` settles cleanly, then re-run the placement check with the same screenplay and clip.
