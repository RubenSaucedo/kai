Placement can proceed from the existing inputs.

- The screenplay, measured take, and narration take are aligned to the same revision.
- All referenced steps are `ok`, and the operator says the interior gate and other relevant states were manually inspected, visible, readable, and correctly represented.
- The narration starts after `edit-name`, so the allowed speech window is `7.4s` to `15.0s`.
- The measured narration clip is `4.2s`, so it fits within the available `7.6s` window.
- No synthesis or finished render is required for this placement-only assessment.

Exact next action: prepare the placement plan from the existing files with the authorized placement command, using the supplied screenplay, measured take, and narration take, and write the placement plan output only.
