Reject placement for `n-1`: the clip length could fit the span, but `edit-name` is unsettled, so the `start_after` gate is not valid enough to place narration.

Exact next action: get a settled measurement for `edit-name`, then re-run the placement assessment against the same measured take.
