Reject the placement: `edit-name` is unsettled, so the narration cannot be aligned from that gate even though the clip duration would fit the measured span.

Exact next action: request a new narration take or a revised screenplay after `edit-name` is settled, then re-run placement.
