No mockup or options exercise is needed here.

Proceed with a bounded copy-only implementation: rename the secondary button from `Create alert` to `Save search` and leave the existing toolbar layout, spacing, and interaction flow unchanged.
