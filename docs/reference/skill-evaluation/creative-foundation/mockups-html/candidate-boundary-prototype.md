This request is outside the HTML mock base. The base is for offline, static visual review of hierarchy, appearance, and layout; it does not implement simulated task flows, Next/Back behavior, inline validation, or transition choreography.

Bounded next step: keep it as a labeled static mock that shows the compact summary step and the detailed review step as separate states, or move to a separate prototype/implementation path if the operator needs interactive flow and transition feel.
