# Mockups HTML candidate boundary: prototype request outside base

## Task

Answer the operator's request. Return only the answer you would give them.

## Request context

- Surface in scope: an onboarding wizard for comparing search-result review paths.
- The operator asks for a prototype that lets reviewers click Next and Back, reveal inline validation, and experience the transition from a compact summary step to a detailed review step.
- The deciding factor is task-flow behavior and transition feel, not only static appearance.
- The operator asks whether this HTML mock method should build that interactive flow today.
- No implementation is authorized by this answer.

## What to answer

Give a concise answer that states:

1. whether this request is inside the HTML mock base;
2. why or why not;
3. the bounded next-step recommendation instead of building JavaScript flow or presenting a static mock as if it were a prototype.
