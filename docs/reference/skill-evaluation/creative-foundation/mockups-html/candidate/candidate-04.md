No mockup/options exercise is needed here.

Recommended action:
- Update the secondary toolbar button copy from `Create alert` to `Save search`.
- Keep the layout, spacing, and interaction flow unchanged.

Caveat:
- The supplied evidence says the new label already fits at 1280px, so no layout risk is expected. If another breakpoint or localization changes the label length, recheck then.