No mockup or options exercise is needed here.

Recommended action: make the copy-only change and rename the secondary button from `Create alert` to `Save search`, leaving the toolbar layout, spacing, and interaction flow unchanged.

Narrow caveat: the supplied evidence says `Save search` already fits at `1280px`, so there is no layout risk to resolve before planning.
