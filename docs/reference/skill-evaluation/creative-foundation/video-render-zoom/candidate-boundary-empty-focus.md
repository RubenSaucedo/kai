Normalized plan: `captures/external-demo.mp4` -> `renders/external-demo-focused.mp4`, `1280x720` at `30fps`, with `focus` count `0`.

Actual focus/cursor outcome: no focus segments are present, so no zoom or crop is applied; no cursor telemetry was supplied, so no cursor path or overlay exists to render or infer.

Exact next action: do not render this plan. If focus is required, provide at least one measured focus segment and any cursor telemetry, then rerun the request.
