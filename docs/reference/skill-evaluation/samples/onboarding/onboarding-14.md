Run `npm test`, which executes `node --test worker-status.test.mjs`.

No extra setup is required from the supplied metadata beyond Node.js `>=24.15.0`; the teammate is already on Node v24.15.0, so that prerequisite is satisfied.
