Run the regression tests with `node --test worker-status.test.mjs` (the `test` script in `package.json`).

No additional setup prerequisite is established beyond Node.js `>=24.15.0`, and the teammate's Node v24.15.0 already satisfies that requirement.
