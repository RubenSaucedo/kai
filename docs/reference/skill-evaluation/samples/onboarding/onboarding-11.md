Run `npm test`, which executes `node --test worker-status.test.mjs`.

No additional setup prerequisite is established by the supplied metadata. The
repo requires Node `>=24.15.0`, and the teammate's Node v24.15.0 satisfies that
requirement.
