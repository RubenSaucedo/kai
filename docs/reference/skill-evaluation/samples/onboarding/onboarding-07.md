npm test runs the regression tests, via `node --test worker-status.test.mjs`.

The only extra setup prerequisite is Node `>=24.15.0`; the teammate's Node v24.15.0 already satisfies it, so no additional setup is required.
