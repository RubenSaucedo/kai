Run `npm test`, which executes `node --test worker-status.test.mjs`.

The supplied metadata does establish one prerequisite: Node must be at least v24.15.0, and the teammate's Node v24.15.0 satisfies it. No other setup is evidenced here.
