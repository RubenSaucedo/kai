npm test runs the regression tests by executing `node --test worker-status.test.mjs`.

The only setup prerequisite shown in the metadata is Node.js `>=24.15.0`; the teammate's Node v24.15.0 already satisfies it, so no additional setup is established.
