The regression tests run with `npm test`, which maps to `node --test worker-status.test.mjs`.

No additional setup prerequisite is established beyond the Node engine requirement in `package.json`: Node `>=24.15.0`. The teammate already has Node v24.15.0, so that requirement is satisfied.
