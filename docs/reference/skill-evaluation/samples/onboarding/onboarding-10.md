The regression tests run with `npm test`, which executes `node --test worker-status.test.mjs`.

No additional setup prerequisite is established beyond Node.js `>=24.15.0`; the teammate’s Node v24.15.0 already satisfies it.
