Run `npm test`, which executes `node --test worker-status.test.mjs`.

The supplied metadata does not establish any extra setup prerequisite beyond using Node.js `v24.15.0` or newer. The teammate’s Node `v24.15.0` satisfies the declared engine requirement (`>=24.15.0`).
