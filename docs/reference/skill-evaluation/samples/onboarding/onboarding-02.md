Run `npm test` (it executes `node --test worker-status.test.mjs`).

No additional setup prerequisite is established beyond using Node `>=24.15.0`; the teammate already has Node v24.15.0, so that requirement is met.
