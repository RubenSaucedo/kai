No split: this is one coherent, safe delivery. The approved change has a single acceptance criterion, no public API or rollout boundary, and only about 40 lines across two implementation files plus tests. The internal helper extraction is a necessary refactor inside the same change, not an independently useful increment, and the repository explicitly permits it. Splitting it would create a coordination-only step without improving compatibility, reversibility, or reviewability.

Validation: keep the helper extraction, the new validation behavior, and the tests together in one PR. Review should confirm both branches now share the internal helper, the new validation is enforced, and the existing behavior remains unchanged except for the added check. Tests should cover the validation path and the unaffected existing path.
